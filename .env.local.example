# Umami Go Environment Variables
# Copy this file to .env.local and fill in your values
# 
# IMPORTANT: These credentials are ONLY used in dev mode and are NEVER bundled in production.
# The app.config.js reads this file and only exposes credentials when NODE_ENV !== 'production'.
# 
# Never commit .env.local to version control (it's already in .gitignore).

# Self-Hosted Configuration (for dev testing)
UMAMI_HOST=https://umami.example.com
UMAMI_USERNAME=admin
UMAMI_PASSWORD=your-secure-password

# Umami Cloud Configuration (for dev testing)
UMAMI_CLOUD_HOST=https://cloud.umami.is
UMAMI_CLOUD_API_KEY=um_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
